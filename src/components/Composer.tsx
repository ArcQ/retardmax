import { useState } from 'react';

type Group = { id: string; name: string };
export default function Composer({ groups = [] }: { groups?: Group[] }) {
  const [body, setBody] = useState(''); const [busy, setBusy] = useState(false); const [message, setMessage] = useState('');
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setMessage(''); const form = new FormData(event.currentTarget); const response = await fetch('/api/posts', { method: 'POST', body: form }); const data = await response.json().catch(() => ({})) as { error?: string }; if (response.ok) location.reload(); else setMessage(data.error ?? 'That move did not save.'); setBusy(false); }
  return <form className="card space-y-4 p-5" onSubmit={submit}><div className="flex items-center justify-between"><div><p className="eyebrow">Your move today</p><h2 className="font-display text-3xl">MAKE IT COUNT.</h2></div><span className="text-sm text-white/40">{body.length}/280</span></div><textarea required maxLength={280} name="body" value={body} onChange={(e) => setBody(e.target.value)} rows={4} placeholder="cold-called 100 people before 9am..." className="w-full resize-none rounded-xl border border-white/10 bg-black/30 p-3 text-lg outline-none ring-flame focus:ring-2" /><div className="flex flex-wrap items-center justify-between gap-3"><label className="cursor-pointer text-sm font-bold text-white/60">+ image <input className="hidden" type="file" name="image" accept="image/jpeg,image/png,image/webp" /></label>{groups.length > 0 && <select name="group_ids[]" className="rounded-lg border border-white/15 bg-black px-2 py-2 text-sm"><option value="">Keep it in the log</option>{groups.map((g) => <option key={g.id} value={g.id}>Share to {g.name}</option>)}</select>}<button className="btn-flame ml-auto" disabled={busy}>{busy ? 'Saving...' : 'Post privately'}</button></div>{message && <p className="text-sm font-bold text-flame">{message}</p>}</form>;
}
